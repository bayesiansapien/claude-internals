---
description: Audit which MCP servers are loaded and what they cost in tokens
---

Run the mcp-audit script and present the output to the user.

```bash
python3 claude-optimizer/scripts/mcp_audit.py
```

Present the output verbatim. If unused servers are flagged, ask the user if they want help disconnecting them.
